# Phase 67 — Pane: successors named by the 2026-09-16 cleanup

## 2623 — COMPLETE 2026-09-17 (GH-PANE-WINDOWS-BYPASS, a fork under the in-session rule; an independent review's five findings fixed before the tick)

**Contract.** Given `pane` started with `--dangerously-bypass-os-sandbox --yolo` on Linux or Windows (macOS refuses by platform), when a tool runs a program on Windows, the child is created by the applier's own `CreateProcessW` with no AppContainer, the confined path's pipes and job, and a `LineShape` command line, and the result carries `Confinement::DangerouslyUnconfined` as on Linux — while a session without the bypass is unchanged and only `Profile::os_sandbox_bypassed()` can select the unconfined path.

**The ruling behind the gate.** The flag was refused on every non-Linux host ("supported only on Linux for externally isolated benchmark/CI containers"); the user's decision 1A ("build it") and the review's finding that the new arm was otherwise unreachable from the shipped binary lifted it to Linux and Windows — the same case, an externally isolated container, VM or CI runner as the boundary (the GitHub runners lack the AppContainer isolation service). macOS stays refused: nothing outside the seatbelt is the boundary there.

**Production.** `sandbox/windows.rs :: platform::spawn_unconfined` (`pub(crate)`, one caller), `platform::start` (the shared body with `container: Option<&AppContainer>`), `SpawnError::NotPrepared` (a failure before the call is "the child could not be prepared", never a permission decision); `tools/invoke.rs :: spawn_bypassed` (Windows arm; the `LineShape` the confined path chose is passed through); `session.rs` (the platform gate).

**Regression.** `tools::invoke::tests::explicit_os_sandbox_bypass_is_never_an_implicit_fallback` (macOS, Linux, Windows); `tools::invoke::tests::a_bypassed_child_reads_what_the_sandbox_would_refuse` — both halves spawn with the same read grant for a file outside the root; the confined child's read is refused by the OS sandbox (exit non-zero, "Access is denied" on Windows), the bypassed child reads it; `explicit_os_sandbox_bypass_refuses_by_name_where_no_applier_exists` for the third arm's platforms; `tests/session.rs::os_sandbox_bypass_is_accepted_on_linux_and_windows_and_refused_on_macos` and `::os_sandbox_bypass_requires_an_explicit_yolo_grant`.

**Review (a fresh subagent, before the tick).** Found: `spawn_unconfined` was `pub`; the CLI gate made the arm unreachable; the decisive test's confined half tolerated a pre-spawn refusal through `unwrap_or(false)` and its profiles differed; the tests lost their `#[cfg]` while the third arm remained; error kinds on the unconfined path said "AppContainer" though none was asked for. All five fixed; the confined path was found byte-for-byte unchanged in checks, attribute list, handle inheritance, job and resume semantics; no leak on the unconfined path (every resource RAII, the handle list still limits inheritance to the three pipe ends).

**Mutation.** `bypass-still-enters-the-container` (`spawn_unconfined`: `start(…, None)` → create an AppContainer and `start(…, Some(&container))`): KILLED on the ARM64 VM by `a_bypassed_child_reads_what_the_sandbox_would_refuse` ("the unconfined child must read the file outside the root: \"\" (exit Some(1), err \"Access is denied.\")"). A first attempt that swapped the call for `spawn` was a COMPILE-ERROR (an orphaned `pub(crate)` fn under `-D warnings`) and is not counted.

**Gates.** VM leg: pane lib 406/406, `tests/session.rs` 69/69, `container_profile` 9/9, 223 targets green (the one red, `competitive_web_runtime.rs`, was the fetch wave's in-progress test rewrite in the shared tree). Host: `tools::invoke` 23/23, `container_profile` 12/12, `session` 107/107, clippy clean; the targeted blast radius on the first report: windows-gnu check clean, rustdoc clean, sizes none grown.

**Limits.** MCP servers under a bypassed profile on Windows now spawn where they refused (`mcp.rs` shares the spawn path; the bypass is explicit and `--yolo`-gated). The bypass skips the writable-program check by design, as the Unix arms do. The x64 `pane (windows-latest)` cell re-verifies on the push; the VM is ARM64.
