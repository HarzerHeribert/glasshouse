**2026-09-14 (full-suite pass paused for two product gaps; acceptance list and stall detection landed — Fable 5.1 primary):** after the ablation, the user asked for the leaderboard-shaped pass: all 89 Terminal-Bench 2.0 tasks once, cells-only. The runner grew to one-attempt manifests (digests computed with Harbor's own `Packager`), the oracle ran 89/89 with 11 official solutions failing here (QEMU under Rosetta, a training timeout, a 403 download, apt pins, tool drift — all attributed, reported, excluded from the scored denominator), and the model run reached 37 trials — **18 verified, 16 of 28 on oracle-passing tasks (about 57 %)**; three provider safety refusals on security tasks are not Pane's and leave the denominator. Three harness defects surfaced and are fixed and committed: the instruction-index scan budget ended a 32-cell task on a 60,000-file dataset (`58c23ee`, a budget is now a notice); the adapter passed an instruction beginning with `-` positionally (`d9b4dd6`, `--task=`); a resumed job carried the old artifact hash (`d52e4db`). The user then ruled: no more runs until the improvements are in, and completion must be verified against a list derived from the request. **Landed today** (`acceptance.rs`, `progress::Stall`; map lines 2595–2596, evidence, design-decisions *Completion is checked against the request, and a stall is a notice, not a cap*): the `accept` helper turns the request into at most eight verifiable items before the first turn, the model sees them, an unmet item holds the completion once and judge items reach the fresh checker with the mechanical results; six cells without a tree change, a new fact or a verification produce a notice and a count, never a stop, and `[limits] cells` is a 120-cell backstop (the adapter follows). Gate green, four mutations KILLED across the day's changes, cells-only default kept. The paused campaign stays unsealed under `campaigns/tb2-pane-full-cells`; the roadmap names the fresh full-suite pass on the new artifact as the next campaign. Public leaderboard context for that report is aggregated in the session's scratchpad (`leaderboard-agg.json`): Terminus 2 baselines 63–65 %, product harnesses 70–78 %, top entries ~90 %. Next action: build the artifact from this commit, oracle, run, `scripts/full_suite_report.py`, and record the score against that table.

**2026-09-13 (interface ablation run; the evidence gate now covers prose completions — Fable 5.1 primary):** the three pre-checks passed (the three final-state fixtures replayed through the installed binary; the Linux artifact of `5290644` built with a canary that now proves a tool ran; container mode proven in the debugging task image — reads outside the root granted, `gdb` admitted under the bypass and refused without it; oracle 4/4), then the campaign specified in the roadmap ran on the subscription route, arms one after another, 1 h 47 min: **cells-only 12/12 verified with 114 parent requests, hybrid 11/12 with 145, tools-only 10/12 with 188** (two frame-limit exhaustions on the debugging task at one operation per frame). Hybrid fails the predeclared non-inferiority test against cells; the default-interface decision is the user's and nothing forces a quota either way. 8.7 M known tokens in all, twice the plan, the excess almost all in the tools-only arm. Record: `pane-benchmarks/TERMINAL-BENCH-RESULTS.md` (*Interface ablation*), `pane-benchmarks/results/tb2-pane-ablation-20260913/` (sealed summaries, hashes, comparison via `scripts/compare_interfaces.py`), roadmap *The 2026-09-13 ablation*, evidence line 2594 (ticked; Phase 65 closed 15/15). One defect found by reading the first live trial and fixed the same day: `prompt::completion_text` makes any prose message a completion and the model ends tasks in prose after a structured return, so the gate wired to terminal returns gated nothing in the field — `TaskState::prose_completion` now runs the same gate on a prose answer (two new binary canaries, mutation KILLED, full Pane gate 92 targets green, ratchet green after the arm moved onto `TaskState`). The campaign's artifact predates that fix, so 28 of 36 completions were ungated prose; the next campaign (roadmap, same section) reruns cells and hybrid on the new artifact with the completion-integrity columns filled. Two small follow-ups the trials name: `rg` should fall back to `grep` where ripgrep is absent (three exit-127 failures), and a cell binding a host name (`checks`) should get the alternative in one line rather than a refusal. Installed after this commit: `scripts/install-local.sh`, and the Linux artifact rebuilt from the same commit for the next campaign. **Decided the same day: cells-only is the default interface** (`abi::Interface::default`, pinned by `tests/prompt_interface.rs::the_default_interface_is_cells_only`; design-decisions *Cells-only is Pane's default interface*; the benchmarks adapter's default follows). Its gate: full Pane suite green except one flaky-pass — `ruler_run::an_unreadable_meter_leaves_the_tokens_absent` red once under the full parallel run (the ruler tests cut git worktrees and run scripts), green alone on the rerun; not attributed to the change, which touches nothing it reads. Next action: the follow-up campaign on the new artifact.

**2026-09-13 (Pane smarter-and-cheaper roadmap implemented end to end — Fable 5.1 primary, nine parallel worktree packages):** every row of `docs/product/pane/smarter-cheaper-roadmap.md`'s register is implemented with deterministic evidence; map **Phase 65** (lines 2580–2594) records the rows and `docs/product/evidence/phase-65.md` is the ledger. Root causes found by reading, not by rerunning the pilot: the `as const` refusal was the free-name scan walking into TypeScript type positions (`runtime/cell.rs`); the "source version changed" refusal after Pane's own edit was `visible_sources` never learning an edit's `after_sha256` (`runtime/state.rs`); `gdb` and `/build` were the never-grantable debugger set and the root-only read rule applied under the explicit container bypass, where there is no Pane sandbox to escape; the hybrid prompt said `execute_cell` was the only native tool. Landed: interface-aware preamble and `execute_cell` description (`model-contract.md` §2.1); the `## Environment` manifest from the compiled profile; container mode (reads span the container minus credential stores and deny patterns, debuggers admitted, launchers refused, writes unchanged, ordinary sessions byte-identical); per-call outcomes in multi-call direct frames plus an `## Effects` section for thrown cells; self-registered mutation versions and atomic multi-hunk `edit({olds, replacements})`; the delta handle table with `keep` as pin; observation dedup (`repeat_of`); reducer routing on `[helpers] reduce_above_tokens` with a positive-saving rule; the task capsule (`## Task`, `CellView.capsule`, `telemetry.capsule`); the evidence gate (final-state contract: stray artifacts, misplaced coverage, stale/undeclared verification; the fresh checker under `[helpers] completion_check`; a held return once, then `completion.verified = false`); the no-progress guard, verified checkpoints and cut-off salvage; the scouting preflight with `preflight_scope = "auto"` (default); interface-origin, failure-taxonomy and recovery-cause telemetry; `pane ruler run --pane-interface hybrid,cells,tools --credit-ratio` with interface regret; the Terminal-Bench adapter's `interface` option and telemetry sections. One ruling at integration: `NoVerification` holds a return only where the project declares verification, because the unconditional form held two existing session fixtures for no evidence. Gate: `cargo test -p pane --no-fail-fast` 92 targets, 1386 passed, 0 failed, 1 ignored; clippy `-D warnings`, fmt and `git diff --check` clean; five decisive mutations KILLED (the table is in the evidence entry). The Phase 59 size ratchet then failed on the integrated tree (`session.rs`, `runtime/isolate.rs`, `runtime/bindings.rs` grown past their baselines, `tui.rs` over the ceiling since before this work), so a Green pure-move package followed: `session/{system,task}.rs`, `runtime/isolate/watchdog.rs`, `runtime/bindings/console.rs` and `tui/lane.rs` — 1,421 of 1,573 added lines are moves, 95 are `pub(super)` prefixes, the rest are `mod`/`use` lines, file headers and the three source scans following their files; the baseline was lowered with `--update`. Installed: `scripts/install-local.sh` runs right after that move commit (it refuses a dirty tree), so the live `pane`, `glasshouse` and `inference-gateway` are the `git describe` of the move commit. **No model-backed campaign ran** (by instruction); the next one is specified in the roadmap's *The next model-backed ablation* — three arms × four tasks × three attempts, about 36 trials and up to ~4 M known tokens on the subscription route. Next action: replay the three final-state fixtures through the installed binary as canaries, then run that campaign and fill the register's measured columns.

**2026-09-13 (Pane smarter/cheaper direction clarified — primary, user ruling):** Pane's advantage is the execution layer beneath provider-familiar behavior: canonical deterministic operations, dependable mutation, exact artifacts and cheap reduction should keep only decision-relevant work in the expensive parent's context. The Terminal-Bench trace contains 91 provider-authored `execute_cell` calls and zero direct familiar calls; this is not a lowering-count artifact because it was measured in raw provider `tool_use` blocks, but it is not itself a defect because the tasks were composed and hybrid exposed both choices. The measurement defect is that final telemetry omitted invocation origin and no tools/cells/hybrid ablation was run. The exact 13-cell failure taxonomy, corrected interpretation, GVS5H-derived task-capsule/verifier ideas, build/debug/redesign table and weighted-cost scorecard are now in `docs/product/pane/smarter-cheaper-roadmap.md`. Next: implement its P0 rows in order, add origin/recovery attribution before optimization, replay the two known failure classes, then run matched interface trials; do not start a larger benchmark first.

**2026-09-12 (official Terminal-Bench 2.0 pilot complete — primary + Sol/Luna helpers):** the accepted fresh campaign completed 12 trials, zero retries and zero exceptions: **9/12 (75%)** overall; debugging 3/3, file processing 3/3, SQLite/gcov 2/3 and C/Python polyglot 1/3. The three genuine failures are one out-of-tree gcov layout and two unreaped `cmain` test artifacts. Known usage is 1,371,727 tokens: Sol parent 1,067,208 over 99 requests; Luna preflight 304,519 over 48 requests. All 147 provider requests succeeded and reported usage, but cache-creation coverage is absent and remains unknown. Twelve preflights, 91 cells/13 failed, 134 tools/11 failed; 27m55.9s campaign wall at concurrency two. Public exact-task comparison is Terminus 2 + GPT-5.3-Codex 14/20 (70%) and Terminus 2 + Opus 4.6 3/4 (75%); harness, effort, environment and trial counts differ, and no per-task actual Codex CLI/Claude Code artifacts were available, so aggregates are not substituted. Oracle 4/4; 205 raw artifacts sealed. Full report: `/Users/eneas/projects/pane-benchmarks/TERMINAL-BENCH-RESULTS.md`. No capability box closes from this pilot. Next: use the repeated cleanup/layout failures to design a verifier-aware completion check, then repeat a larger matched trial set before any ranking claim.

**2026-09-12 (Terminal-Bench execution canary repaired — primary + Sol design audit):** the official oracle passed all four selected tasks, then the first model-backed canary exposed two adapter/runtime defects before a campaign was accepted: trial metadata was serialized before enrichment, and Pane's fail-closed Landlock/seccomp path refused every tool inside OrbStack's nested Linux containers. Both launches were stopped and excluded as infrastructure-invalid; no 12-trial result is claimed. Pane now has a Linux-only `--dangerously-bypass-os-sandbox` requiring `--yolo`, available only as a CLI choice, never as configuration or fallback. Admission checks, credential stripping and cancellation remain; the outer benchmark container/VM becomes the named OS boundary. The reproducible Linux artifact builder now performs a no-provider real shell-tool canary, so a version-only success cannot start a paid campaign. Focused bypass and adapter contract tests pass. Next: commit, rebuild/hash the amd64 artifact, pass its local tool canary, initialize a fresh campaign, run exactly 12 valid trials, seal and report.

**2026-09-12 (Terminal-Bench telemetry and paired pilot prepared — primary + Sol/Luna/Terra workers):** Pane's final `json`/`stream-json` result now carries task wall time, cells and failures, direct tool calls and failures, every preflight helper record, provider request/response/coverage counts, and disjoint input/output/cache-read/cache-creation usage split by parent/helper and model. Missing usage remains uncovered rather than zero. Six machine-output regressions, the full native Pane suite and the adapter's offline contract tests pass. The separate `pane-benchmarks` checkout pins official Terminal-Bench 2.0 revision 1, four medium tasks (debugging, software engineering, system administration and file operations), three attempts, official timeouts, no retries and concurrency two; Harbor alone grades them. The task container receives a hash-pinned Linux Pane binary and only a short-lived proxy bearer; the real ChatGPT subscription gateway credential remains on the host, with no API-key fallback. A real Harbor config parse and authenticated Docker-to-host bridge smoke pass. Public paired baselines are inspectable Terminus 2 + GPT-5.3-Codex and Terminus 2 + Claude Opus 4.6 artifacts for the exact tasks; actual Codex CLI/Claude Code public rows expose aggregates but no per-task trials, so they will not be substituted. No capability box closes from this benchmark preparation. Next: finish the cross-platform gate, build the committed amd64 artifact, require the official oracle at 4/4, run exactly 12 Pane attempts, seal artifacts and append the measured comparison.

**2026-09-11 (green CI: reds stop nothing, a failed test is rerun alone — Fable 5.1):** the user asked why three cells stay red and pointed out the real cost: a red Test step stops the cell, so a load-flaky pty test hides the clippy and rustdoc reds behind it. Landed: both sweep jobs tee their `--no-fail-fast` log and `continue-on-error`, doctests/clippy/rustdoc always run, and a final step runs `scripts/ci/rerun-failed-alone.sh` — every failed test rerun alone once by its own binary, `flaky-pass` named, `still red` fails the job (tested in `scripts/tests/test_rerun_failed_alone.py`). Two tests adjusted on their own evidence: `terminal_loss`'s 4 ms resize gap is measured and printed, not asserted (its own doc says it cannot discriminate under load; GitHub macOS swallowed 2 then 5 of 8), the 500 µs gap stays the proof; the helper cancellation bound is 1 s against a 3 s hold. Two fix-forwards on the first sweeps: `4f5f5cd` (the rerun step parsed nothing through `CARGO_TERM_COLOR=always` colour codes and CRLF and exited 0, turning the red Windows pane cell green — now stripped before parsing, backslash paths run with forward slashes, and nothing-to-rerun fails closed unless the log is clean) and the next commit (`terminal_loss`'s 500µs gap swallows 2, 5, 4 of 8 on GitHub's macOS runner with the fixed tree, rerun alone included, inside the reverted line's 5–8 of 8 — `host_carries_the_proof` measures and prints it on a GitHub runner and asserts it everywhere else; the proof stays with the local gate). The Windows pane cell is now honestly red: 18 real failures, each rerun alone and quoted, grouped in the checkpoint as 13 empty file reads/searches through the read authority, 3 unreachable loopback fakes, 1 sandbox write denied. The last non-Windows flake, pane `tui_live::telemetry_and_motion…` (red alone on the Ubuntu runner on `b733245`), was reproduced locally at 5 of 12 under 12 busy loops and run to its root cause with fixture instrumentation: the Escape was always sent before the resize's own redraw, and crossterm 0.29's mio source returns a resize without reading the tty bytes the same poll reported — edge-triggered, so the key is stranded until the next keystroke (the defect Glasshouse's `tui/event.rs` already narrows from its own loop). The fixture's `resize()` now waits for ratatui's clear before returning, which removes the test's race; the product side — a key typed while pane is drawing a resize arrives with the next key, in order — is a curiosity, not a defect (user, 2026-09-11: nothing is lost and nobody types mid-resize), recorded here once and not packaged. Not yet addressed: the Windows pane cell's standing failures (a Windows packet on the ARM VM) and the Pane `tui_live` Escape race under load (a determinism packet; it now shows as flaky-pass rather than a red cell).

**2026-09-11 (one store — subscription/account/broker state is the gateway's; Fable 5.1):** user ruling after the standalone dogfood found the connected subscriptions invisible to the gateway (two stores, not invalid logins). Landed as `3780be0` `24a5dd7` (gateway: `credentials set/remove --variable`, `subscriptions logout|adopt-binary`), `eb0d470` (Pane: account/subscription/credential controls run the gateway binary in every session; `routing-cost` stays Glasshouse's when hosted), `9d522cc` (Glasshouse: `[entitlements.*]` is a policy overlay keyed by gateway account names, the five account keys refused by name; `GatewayCatalogue`/`Layers`; `RuntimePaths` derives brokers, tools, model catalogues and the health/quota caches from the gateway's data dir; `subscriptions connect|login|logout|adopt-binary` and `credentials store|remove` forward to the binary; `glasshouse migrate-gateway-state [--dry-run]`), `69e61be` (Glasshouse resolves credentials through the gateway's file first), `71cd4e9` (Pane lets the gateway shut down before killing it — three brokers were orphaned per session end), `2aa717c` (record). Executed on the user's machine: seven accounts, config rewritten to the overlay with a dated backup, four directories renamed into `~/Library/Application Support/inference-gateway/`; `pane` in an empty directory with no environment variable then answered a real turn on `claude-max`. Deliberately not moved: the provider serving keys (`template`, `base_url`, `credential_env`, `credential_store`, `headers`) — the Settings provider editor and the onboarding wizard author them; the successor package is named in the record. User instruction the same day: the Gemini subscription is not used through the broker (Google bans it outside the Gemini CLI); its account table was removed from the user's `gateway.toml`, login state kept, Antigravity's own sign-in still resolves. Two of the user's day-old sessions still run brokers from the old paths and need a restart. Next action: the provider successor package; then a rebuild for a serving gateway when a subscription connects; then hosted Pane's `/key` end-to-end on the installed binaries.

**2026-09-11 (standalone bootstrap: the gateway listens before it has a key, and Pane stores one — Fable 5.1):** the first standalone dogfood of the extraction died at start (`pane` in an empty directory, no key exported: the gateway refused to serve with nothing to forward to, its message joined fourteen providers into one sentence, and a Groq key filed in the Keychain by Glasshouse was refused to the gateway — ACL bound to the creating build's code hash, dialogs silenced). User ruling: enter the key in Pane's TUI and have the gateway store it. Landed: `gateway::UpstreamSlot` + `start_awaiting_upstream` (listen first, `503` with the refusal after the bearer check, one rebuild per second, filled once; hosts never deferred, `Gateway::upstream()`/`serving_provider()` now `Option`); `secret::file` — `<data dir>/credentials.toml`, `0600`, store chain file → native → environment; `inference-gateway credentials list|set|remove` (key on stdin, names only out); Pane `/login` lists API-key rows, `/key <provider>` masked modal prompt piped to `set`, start-up notice when nothing is stored. Record and residuals: `design-decisions.md`, the 2026-09-11 addendum under *The inference gateway is its own crate and process*. Dogfooded on the installed `v0.1.0-pre.1-170-g80bb1ca`: the user's three Glasshouse-connected subscriptions were invisible to the standalone gateway (two stores, not invalid logins); bridged with a mirrored `gateway.toml` and `INFERENCE_GATEWAY_DATA_DIR` pointed at Glasshouse's data directory, one real turn completed through `claude-max` standalone. Two defects fixed forward (the notice ignored connected accounts; the broker executable's flat fallback path). Next action: the *one store* package (Red) named in the record's 2026-09-11 addendum; until then the launcher in the session scratchpad carries the two variables.

**2026-09-10/11 (gateway extraction, Fable 5.1 lead, branch `gateway-extraction`):** the user's three-component ruling executed end to end — `crates/inference-gateway` (library + `inference-gateway` binary) holds the serving path, credentials, entitlements, same-model failover and the wire vocabulary; Glasshouse depends on it and keeps sessions, the ledger, the scorer and harness identity; Pane spawns or attaches to the binary. Seven preparatory cuts landed on `main` first (`c744891`…`744cec0`); the move replayed as renames. Boundary scans in `gateway/tests.rs` and `main.rs` forbid every host module and the literal `glasshouse::`. Three lead decisions are recorded for the user in `design-decisions.md` (*The inference gateway is its own crate and process*): the ledger-read side of failover ranking left with the cut, Pane runs direct when no gateway is installed, the standalone gateway marks every account metered. Verification and the three fresh reviews are in the same section. Merged fast-forward as `40efc65`…`767fb10` (six commits) and installed as `v0.1.0-pre.1-165-g767fb10` for all three binaries (`~/.local/bin/inference-gateway` is new). The three fresh reviews' findings and fixes are in the record's *Three fresh reviews* paragraph; the local gate on the final tree was green except `test_launch_prompts.py` (worktree-environmental) and two `ruler_run` flaky-passes. The trailing GitHub sweep on `78640fb` (run 34542220044) was red in six cells, attributed: pane macOS/Linux — `model_picker_names_the_active_slug_without_calling_the_provider`, red on the run before the merge too and green locally only because the developer's own `glasshouse` answered; fixed forward by giving the test its own fake Glasshouse under the hosted rule. Everything else is not this work's: macOS `terminal_loss::a_resize_does_not_swallow_the_keystroke_that_follows_it` (both macOS cells, red before the merge — the load-sensitive family), `commands::subscriptions::tests::nonzero_and_timeout_are_bounded_without_child_output_in_errors` (a 20 ms timing bound, red on the Linux MSRV cell and green on the declared one), `seams.rs::a_tool_result_goes_to_the_context_firewall_not_the_lifecycle_hook` (reads the hook's record file before the hook wrote it, Linux only), and the Windows pane cell, whose failing set differs run to run (`search_artifacts` 0/5, a `tui_live` wheel test here; direct-provider and checker tests on the run before). Next action: dogfood Pane standalone through the installed `inference-gateway` (a real turn on a real key, then a subscription account); the successor lines are a gateway-owned in-memory evidence store and health/cost in the standalone ranking.

**2026-09-10 (helper preparation, profile picker and checker handoff installed and verified):** Both binaries are installed from `6d0cbe3` as `v0.1.0-pre.1-131-g6d0cbe3`. Real Sol/Luna expense repair passes ten project tests and 16 frozen independent cases with zero operator corrections; follow-up installed review passes in four cells, correctly distinguishes fresh/reused checks and interprets Luna before the final verdict. Full Pane suite: 925 summed passing executions, one ignored; scoped gate and Clippy pass. Prior full corrected Glasshouse workspace passes. The completed review is visible in dedicated cmux workspace:234. Known coding-trial usage is 118,529 (84,753 Sol + 33,776 Luna), with partial cache coverage and no monetary estimate. No capability closure. Next product work: representative additional tasks, broad parent-glob context selection and remaining physical text-selection/native-terminal verification; web crawling remains deferred. Details: `docs/process/dogfooding-2026-09-10-helper-preparation.md`.

**2026-09-10 (helper and console repairs installed; real tasks verified):** `b2c38db` is installed as `v0.1.0-pre.1-128-gb2c38db` for both binaries. The realistic Sol/Luna expense repair passed nine project tests and 16 independent cases without an operator correction; the installed formatter replay then preserved the complete structured README and ran nine tests twice through one persistent verifier in 25.8 seconds. Final Pane suite: 894 passed, one ignored; independent Sol reviews ACCEPT. Prior unchanged Glasshouse workspace evidence covers 4,237 active tests. Reported full expense-task usage is 201,801 including helpers/cache; UI currently shows parent usage only. Next action: restore desktop access and finish cmux mouse/focus validation, then address the known quick-open profile and helper-spend telemetry gaps in map order. `cgWindowNotFound` still blocks desktop proof; no new Windows proof or capability closure. Details: `docs/process/dogfooding-2026-09-10-helpers.md`.

**2026-09-10 (Glasshouse/Pane input repair installed and dogfooded):** `f912bb9` is installed as `v0.1.0-pre.1-126-gf912bb9` for both binaries. Two isolated GPT-5.6 Sol editors and one Sol reviewer delivered immediate embedded focus, viewport refocus, child mouse/paste forwarding, Pane picker clicks and bare `pane` launching its composer. Four real installed-binary subscription tasks passed: Claude initial/follow-up (21 → 34 after an external file edit), Gemini (21) and OpenAI Sol (21), with exact output bytes and native read/write/read calls independently checked. The report is `docs/process/dogfooding-2026-09-09-input.md`; no capability boxes changed. The completed OpenAI test session is left visible with an empty composer. Next action: address the separate TUI quick-open profile-selection gap (the CLI `launch --profile` route works), and verify physical Ctrl+5/Ctrl+6 and precise mouse/selection behavior outside the current computer-control restrictions.

**2026-09-07 (matched subscription benchmark and durability correction):** Two autonomous Claude Code controls passed the same six-case oracle through `claude-sonnet-4-6`/`claude-max` in 13–17 requests, 279–306 s and 372k–444k reported tokens. Pane failed 4/6 before the correction and again after it: the post-fix attempt improved to 15 requests, 134 s and 247k tokens, but its project-local self-tests missed macOS `/var` → `/private/var` identity. One operator diagnosis let Pane repair and pass in ten more requests; that 25-request, 529k-token result is explicitly assisted. `0760c14`, `8c77e4d` and `31aeaec` remove all token-based task stopping, add literal line-array writes/edits, and put recovery guidance in every undefined-name error. The full Pane suite, Clippy, formatting, Windows GNU test compile and targeted blast-radius gate are green. The visible assisted session remains open in cmux `workspace:216` / `surface:232`. Next action: make autonomous verification exercise real platform boundaries, then repeat across different tasks and trials before any superiority claim.

**2026-09-07 (Pane resilience register and supervisor truth):** The user's ruling moves model-specific failure handling out of the stable system prompt and into schemas, validators, runtime diagnostics, typed environment context and the supervisor. `docs/product/pane/improvement-register.md` records the full P0–P2 table: detection/correction, the one-sentence next-turn feedback and evidence needed to close every item. Phase 61H is open in the capability map. The supervisor is wired but was not running in the benchmark (`enabled = false`); Pane otherwise defaults to no supervisor model and does not prove that a configured model is smaller. Next action: wire eligible lower-cost model resolution, make looks visible and prove a live loop intervention, then execute the register's P0 rows in order.

**2026-09-07 (live structured-return correction):** The visible Gemini subscription session exposed a terminal-control defect after two evidence-gathering cells: Gemini returned `{matchesCount: 0, sampleMatches: []}` as notebook inspection, and Pane promoted that object to the assistant's final answer. Pane now treats objects, arrays, collections and host tool objects as bounded `## Output`, renders them as a distinct notebook region, supplies them to the next provider turn, and reserves ordinary completion for text/scalars; both foreground sessions and `agent.run` use the same predicate. The regression reproduces the exact object and proves a second request plus final prose. Full Pane tests exposed one stale model-picker expectation from the prior active-provider change; that fixture now opens on Google, the traced gate and Clippy pass, and Windows cross-checks clean. Live replay through `subscription-gemini-pane` in cmux `workspace:208` completed with three recommendations after 11 evidence cells instead of stopping early. It also measured the next defect: a 70.7 s broad-document task consumed 369,178 reported tokens because Gemini repeatedly paged through the 20,086-line design ledger. Next action: constrain repository context selection before benchmarking quality or token efficiency.

**2026-09-07 (subscription broker and Pane model picker production closure):** Phase 56/56A had the resource model and protocol gateway but no production OAuth-account upstream. `97b5946` + `62e9d1e` + `b2d95fc` + `0f939c3` add the isolated CLIProxyAPI runtime, routing and account verbs; live dogfooding found malformed nested YAML and premature HTTP readiness, fixed in `7fb91fa`. Google, Anthropic and OpenAI report present in Glasshouse-owned entitlement directories, and real Pane sessions `9b98f0dff9d6`, `7c0b32872b7d`, `b1ffd6f222db` returned exact markers through Gemini 3.8 Flash, Claude Sonnet 4.6 and GPT-5.6 Sol. `5bc9244` + `c7c886b` + `b865c68` replace `/model`'s flat list with a theme-colored provider carousel, live global search, exact model selection, and honest locked-account rows. `c1c131c` supplies the missing live broker catalogues and a nonsecret active-entitlement marker: the real catalogue now reports Google 11, Anthropic 16 and OpenAI 9 account models, while preserving the 444 API-provider entries. A visible Glasshouse-launched Pane session in cmux `workspace:207` returned `LIVE SUBSCRIPTION OK` through `gemini-3.8-flash-high`; its `/model` panel is left open for interaction. Next action: run the requested fixed-task benchmark from clean rollouts and record tokens per completed task, wall time, outcome and tool-round behavior.

**2026-09-05 (build hygiene, the repository made public, and three findings — Fable 5.1 session, branch `claude/ci-speedup`, uncommitted):** the local gate was measured, not assumed: **cold, three platforms, from a wiped tree: 2371s; warm macOS: ≈300s, of which ≈175s is test execution** across 136 sequential binaries (median 0.51s — process startup, not test work); **Windows VM leg alone: 1089s**, build and MSRV green, test red only on the two findings below. Four defects in the build itself, all fixed on the branch: (1) `RUSTFLAGS` carried three different values across gate steps and interactive builds, so one `target/` held **1203 fingerprint variants of `glasshouse` and 24 of `thiserror`** — 98.7 GiB of coexisting builds; warnings now come from `[workspace.lints.rust]` and every invocation shares one namespace. (2) **The macOS leg never used the declared toolchain** — `[workspace.metadata.ci] toolchain` pinned only the Docker image; macOS built with Homebrew's 1.96.1, rustup's `stable` was 1.94.0, and 1.98.0 was not installed. `ci-local.sh` now prepends the declared toolchain's own bin; `rust-toolchain.toml` pins it for rustup shims; Homebrew `rust` is unlinked and `rustup` force-linked so one owner remains. (3) Twenty worker worktrees shared no build cache — `scripts/lib/accel.sh` enables sccache (`~/.glasshouse/sccache`, never idles out) in `ci-local.sh` and `blast-radius.sh`, no-op where absent. (4) The Windows batch carried the same `RUSTFLAGS` split. Also: `ci-local.sh --scoped` (lints + blast radius, macOS-only, refuses platform legs) and **`.github/workflows/ci-extended.yml`** — 12 cells (5 OS/arch × declared + MSRV, plus beta/nightly), real harnesses installed via npm so `Declared` facts are re-verified live, `--no-fail-fast`, RustSec audit — on `push` now that **the repository is public (user instruction, history pattern-scanned clean)**. A nextest attempt was backed out: its serial lane matched 7 targets where `blast-radius.sh`'s classification matches 112.

**Three findings on `main`, none from the branch — the list:**
- **Windows: `glasshouse claim` refuses every non-verbatim path.** `project/mod.rs:187` canonicalizes the root, which on Windows yields `\\?\C:\…`; `commands/sessions.rs::claimed_path` builds the candidate from `current_dir().join()` without it; `commands/context_firewall.rs::project_relative_path` compares lexically and the `nth(1) == ':'` branch refuses. 16 of `file_claims`' tests fail on the VM. Same function serves the firewall's tool-input paths, so file-aware memory on Windows may silently see nothing — inferred, not yet shown. **Red** (project isolation, platform). Fix belongs in `project_relative_path`, once — its own doc comment says a second implementation is how the two callers would diverge.
- **Codex catalogue drift:** installed 0.153.3, `CATALOGUE_OBSERVED_VERSION` 0.151.0 (pinned `c6dc7af`, 2026-08-30). The tripwire working as designed: Glasshouse subscribes to Codex hook *names*, so a Codex release must be re-read before the pin moves. Re-read the hook review screen, reconcile `HOOK_EVENTS`, bump. Small; product, not process.
- **`pty_smoke` fails from inside any Claude Code session** (`tests/pty_smoke.rs:3526`): Claude Code exports `ANTHROPIC_BASE_URL` into every child, the test asserts a launch overlay never leaks it into the parent. 75/76 with it set, 76/76 with `env -u`. Correct test, dirty caller — and every worker pane is such a caller. `ci-local.sh` warns by name. **Decision owed:** scrub inherited provider vars for workers, or accept that the gate is run from a clean shell.

Next action: review and commit the branch; then the Windows claim defect as a Red packet; then `pane` — the first-party harness (design in the artifact *The Glasshouse Native Harness*; needs a `design-decisions.md` entry and map lines before its N1 packet).

**2026-09-03 (user steering decisions at `2d1dc4d`):** four decisions of record in `design-decisions.md` (*Steering decisions of record*). **P1b approved** — the gateway may parse supported relayed bodies for usage/timing under stated constraints, built as one producer then its consumers (~20 lines). **442 approved** — Secret Service keyring behind a platform-neutral boundary, opt-in plaintext fallback only (releases Phase 9E). **Phases 52/53 are deferred experiment gates**, not blockers; a bounded vector experiment is permitted behind an optional boundary, A/B'd against lexical+rerank. **Phase 60 — Parallel-session file coordination** promoted from Maybe A/B/C/F/H as one vertical capability, 16 committed lines, order A+F → B → C → H. The proportional-assurance guidance is downgraded to advisory (it came from a monitoring agent, not the user). Denominator now 1371.

**2026-09-03 (Opus 5 orchestrator, wave 115):** **line 2049 closed — Phase 59's code work is done.** Twelve inline test modules moved out of production files (the last, `events/mod.rs`, by the orchestrator); `--lib` counts identical on both sides everywhere. **Windows VM run 16 on `adc988b` fully green** (build/test/msrv, 2,077 lib tests, zero failures) — the acceptance for the gateway close-sequence fix, whose defect had failed run 14 and its rerun-alone. Phase 59 now holds only 2053, kept as a standing rule rather than a crate-wide trimming sweep, and 2054, the seven re-opened refusals, which is product work. Next: the first dogfooding session, then 2054. Next action: `.agent-runtime/CONTINUATION.md`.

**2026-09-03 (Opus 5 orchestrator, wave 114):** **line 2047 closed** — `scripts/file-size-baseline.txt` is empty: no production file exceeds 2,500 lines. Seven packages under one gate: the Windows gateway close-sequence fix (its Phase −1 producer was falsified by the worker; the real mechanism is a close over an unread receive queue), `shell/mod.rs` trimmed 2,942 → 2,499, four tests-out moves taking line 2049 from 25 files to 11, and `GH-GATE-RERUN-ALONE` making the flake rule mechanical. Next action: `.agent-runtime/CONTINUATION.md`.

**2026-09-03 (Opus 5 orchestrator, morning):** wave 113 — `main.rs` 16,243 production lines → **883**, its 280 items into 21 `commands/` files (**line 2048 closed**); `evaluation/mod.rs` → six files; `session/store.rs` → four; `routing/disposable.rs` → four. The size ratchet went **5 files over 2,500 → 1** (`shell/mod.rs`, whose trim is accepted and integrating next). Two `tests/` scans of `main.rs` were repaired at integration — one had gone red, the other stayed green while covering a twentieth of the code it was written to check. Next action: `.agent-runtime/CONTINUATION.md`.

**2026-09-03 (new requirement, and a process ruling):** Phase 59 — Decompression (map lines 2043–2054, eight lines) — recorded from the user's ruling that the biggest risk is now complexity through over-assurance, not missing quality: split the monoliths, trim narrative comments, run real long sessions, close open lines by risk. `CLAUDE.md` (*Decompression*) carries the seven process changes; `scripts/check-file-sizes.py` is the ratchet in every gate. Denominator now 1355. Phase 59 outranks every feature package.

**2026-09-02 (new requirement):** Phase 58 — context economy learned from a side-by-side comparison with Headroom (map lines 2014–2040, 15 lines) — recorded from the user's instruction (*"take everything which would benefit us in a meaningful way and ingest it … make sure this is documented going forward"*); `design-decisions.md` (*Headroom, compared*) has the comparison, the refusals by name and the order of work. Denominator now 1347.

**2026-08-31 (requirement refined):** Phase 56A — entitlement pool and subscription broker (map 1962–1974, thirteen lines) — recorded from the user; it is the core of Phase 56. Denominator now 1305.

**2026-08-31 (hand-off):** the Opus 5 orchestrator handed off HOT to a Fable 5 orchestrator at the user's request — 1037/1292 committed (`d584ad3`), 1480 in flight behind a gate, subscription-rules (Phase 56 #1) live, wire-file-memory reported with its patch staged. Everything is in `.agent-runtime/CONTINUATION.md` and `.agent-runtime/handoff-batch59-60/`.

**2026-08-31 (new requirement):** Phase 56 — harness–subscription decoupling — recorded from the user's instruction (map 1945–1956, twelve lines; design-decisions has the ruling and the order of work). The mandatory denominator is now 1292.

**2026-08-31 (Opus 5 orchestrator, later):** 1027/1280 committed (`afd9ebb`) — batches 57 and 58 landed, 130 boxes today; one worker live (1317); next: the map side-effect recon and its prove-it successors.

**2026-08-31 (Opus 5 orchestrator):** 953/1280 committed; batch 57's five
remaining packages merged and staged behind one gate (+52 → 1005 when green);
migration ladder now 18 → 19 → 20 → **21**; Phase 21H/I/J and Phase 17/54
complete; three Fable packets (escalation, affinity, route-correlation)
validated and waiting on the commit. Next action: `.agent-runtime/CONTINUATION.md` §1–2.

# Glasshouse implementation handoff

> This describes how Glasshouse is built, not what Glasshouse does. Nothing
> here is a product requirement. Capability requirements live only in
> `docs/product/capability-map.md`.

Last updated: 2026-09-07 (Europe/Berlin)

## Checkpoint — 2026-09-07, matched benchmark observed after Pane correction

- The benchmark used one frozen 1,005-byte prompt, fixture, oracle, `claude-sonnet-4-6`, `claude-max` and Glasshouse gateway. Claude Code passed both autonomous trials: 13 and 17 requests, 306.4 and 278.6 s, 371,863 and 443,606 reported tokens, with peak prompt footprints near 35.5k of 1M.
- Pane trial 01 failed 4/6 after 29 requests and 651.3 s. Its task stopped at the removed spend cap after 980,778 tokens visible in gateway accounting; the latest request occupied only 54.1k of context. Pane trial 02 after the durability fixes reached an autonomous answer in 15 requests, 134 s and 246,918 tokens, but still failed 4/6.
- Active observation separated script-authoring recovery from answer correctness. The line-array guidance recovered the first `${BASH_SOURCE[0]}` interpolation mistake on the next turn. Pane's own project-local tests then missed macOS logical `/var` versus physical `/private/var`; the external oracle caught it. One operator message containing that exact fact produced a correct assisted result after ten more requests, for 25 requests and 529,458 tokens total. Assisted and autonomous scores remain separate.
- Cumulative token spend is telemetry only: no denominator, prompt alteration, subagent refusal or task stop. Current and peak prompt context remain a separate 1M-window meter. Per-cell wall-clock and runtime safety limits remain.
- `write({path, lines})` and `edit({oldLines, replacementLines})` preserve shell text without JavaScript template literals, normalize one line ending, reject embedded line breaks, and emit direct recovery guidance. Existing byte-exact string forms remain.
- Full `cargo test -p pane --no-fail-fast`, all-target Clippy with warnings denied, formatting, Windows GNU test compilation and `scripts/blast-radius.sh --targeted` are green. The report is `.agent-runtime/benchmarks/2026-09-07-subscription-context/reports/summary.md`; the visible Pane session remains open in cmux `workspace:216` / `surface:232`. Next action: add representative platform-boundary verification, then repeat different tasks and trials before claiming parity or superiority.

## Checkpoint — 2026-09-06 (10:40), waves 146–148: 1348 / 1413 (95.3%) — the inbox ruled and its Glasshouse half dispatched, 2475/2481 closed, fix-4 merged, the 2463 preview seam found by its verifier, the sixth build rule

- **State:** `main` at `91baf2f` = `3d0999b` (2475/2481 tick, pushed 10:28; sweep `34021831257` runs on it) + `7c1dabf` (CLAUDE.md's sixth build rule, the waves 146–147 ledger row) + `91baf2f` (pane at `4ab1876`, the isolate fix-4) — **the merge is held unpushed until that sweep concludes**; the Fable 5.1 successor took over at 10:11 after the five-hour reset (the predecessor's tick job died with its pane; the tick was run by hand on `34020972140`'s green). Live: `health-observation-origin` (1325, Amber, Sonnet medium, dispatched 10:14), `api-inbox` (`GH-API-INBOX`, Red, Opus high, dispatched 10:30 — `glasshouse api socket-path`, `from` on `SendMessage`, a `session_messages` table at schema 29 read by `Request::Inbox`, delivery decided by the recipient's harness), the fifth pane lead with `pane-bg-exit-and-cost` (2477's `bg::shutdown` Blocker) and the fifth isolate verifier (2461), and the 2463 preview-seam fix as the lead's next Red packet.
- **Ruled:** 61G §7's two Glasshouse-side gaps (`ruling-inbox-glasshouse-side.md`): the discovery verb, attribution-not-authentication `from`, a new table rather than door memory (two doors share only the database) or the lifecycle ledger (refused on record), a per-session `Inbox` request rather than a third cursor on `Events`; the pane half follows the merge. 2463 (`ruling-2463-preview-seam-and-merge-4ab1876.md`): `PostToolUse` carries the runtime contract's §3 rendering the model sees — one renderer, two consumers — pane-side only, the firewall untouched; ticks on the fix, the hook-seam verifier re-run and green pane cells. 2477 stays LOCALLY VERIFIED until the exit-path fix and one more read-only look.
- **Build rules:** the sixth — pane's Windows test compile is verified only by the `pane (windows-latest)` cell; `cargo check --tests -p pane --target x86_64-pc-windows-msvc` fails on this host inside `ring`'s C build (tried once), so a `cfg`-gated test's helpers and imports are gated by reading before a merge ask.
- **Parked with the user:** `repository-licence-files`, `macos-signing-identity`, `fable-limit-relaunch-model`.
- **Next, by risk:** push `91baf2f` when `34021831257` concludes; the `api-inbox` report → integrate → a read-only verifier through the binary → the lead's pane half of 2479; the `health-observation-origin` report → 1325's tick with the two no-producer register rows (*repair attempt*, *explicit probe*); 514's `GH-SESSION-MIGRATION` after `api-inbox` frees `main.rs`/`cli.rs`; the next dogfooding session's probe (one `Edit`, then `<state dir>/logs/glasshouse.log` for the firewall's WARN line; and whether a confined `bash` needs an exec-root for its own `PATH`).

## Checkpoint — 2026-09-06 (08:00), wave 145: 1342 / 1413 (95.0%) — three pane merges, decision 11's path proven in the third dogfooding session, the credentials verb's sweep red fixed forward, the Fable limit near

- **State:** `main` at this commit = `ee7b1cd` (pane at `9c4e4ef`: the isolate fix-2, 61F, `pane --version`, the Windows build fix-forward) + the credentials name guard, the README's *Provider keys*, the third dogfooding entry; pushed with this checkpoint. **08:20:** sweep `34015246338` green on all seventeen cells → **2485 and 2469–2471 ticked (1346/1413)**; landed since: the consent notice (`23527bd`), the purpose header (`3e164dc`), the revoked-terminal fix (`a920aab`, verifier live), the `file_touched` probe recorded (`2dac933`). Live: the fifth pane lead (Opus xhigh — the Fable weekly limit read 92 % at 07:22, resets Sep 8; relaunch model parked as `fable-limit-relaunch-model`) inheriting `pane-61e-verify-fix-2`, `pane-tools-reap` (reported), `pane-sigint`; `GH-ATTACH-REVOKED-TERMINAL` (Red, Opus); `GH-GATEWAY-PURPOSE-HEADER` (Amber); `GH-EXTRACTION-CONSENT-NOTICE` reported complete, integrating next.
- **Ruled:** `--no-turbofan` stays; the supervisor purpose header as proposed; the inbox door (`ruling-events-inbox-door.md`): `control.sock` with the peer-uid check, mirror `api/client.rs`, poll `Request::Events`, standalone `pane.sock` out. Pane merges are accepted on the lead's `cargo test -p pane` when nothing outside `crates/pane/` changed but `Cargo.lock`; the sweep's pane cells are the verdict.
- **Dogfooding (third session):** the Keychain key reached the hook and Groq answered — decision 11 holds end to end; the blocker in all three sessions was that no `memory_extraction_model` was ever configured, and the outcome row blamed credentials (`GH-EXTRACTION-CONSENT-NOTICE`); `llama-3.1-8b-instant` is gone from Groq (config moved to `openai/gpt-oss-20b`); two orphaned launchers with revoked stdio (`GH-ATTACH-REVOKED-TERMINAL`); `shadow` wrote no `file_touched` row — one probe owed.
- **Trailing sweep:** `34013598118` on `98f61c9` red on eleven glasshouse cells (one test: `credentials store ""` filed by two stores) and the pane Windows build — both fixed forward in this push.
- **Parked with the user:** `repository-licence-files`, `macos-signing-identity`, `fable-limit-relaunch-model`.
- **Next, by risk:** integrate the consent notice; the lane's verifier verdict → 2461/2463/2485, 2469–2471 with the phase-61 entries; the revoked-terminal and purpose-header reports; the `file_touched` probe in the next dogfooding session; tomorrow's dogfooding on the pre-release binary.

## Checkpoint — 2026-09-06 (03:10), wave 142 in flight: 1331 / 1413 (94.2%) — the credential strip's verifier finds the hooks, the user asks for a key-leak guard, the benchmark's meter is the ledger

- **State (07:00):** `main` at `7708071`, 1342/1413; **`v0.1.0-pre.1` tagged** (decision 10; the pane Windows cell green on `34011829488`, the release workflow proven by dry run `34011831478`). Since 06:25: the extraction-outcome row (`ea5a349`), 1210 + the release workflow (`74ff128`), the pane branch merged again at `affba0d` (61G's window; 2476, 2462, 2465 ticked on the Red verifier's ACCEPT of the loop — the isolate fix REJECTED, fix-2 live), the strip's one escape fixed forward (`33486e6`), the key-leak guard live (`1664777`), and the second dogfooding session, which found decision 11's keychain path broken on macOS (`GH-CREDENTIALS-STORE-VERB`, Red, live). Earlier: `main` at `e3297cb`. Landed since 03:10: `9cdfb3d` (the pane adapter's describe, the deviation ruled), `3b9a0b0` (**488 re-ticked — Phase 9G complete**; the verifier ACCEPTED after fix-forward 2 made the hooks' loss loud; **six disputed lines closed on demonstrations — Phases 15, 20 and 31 complete**; 1338/1413), `7d84b6c` (`NothingConfigured` names the real knob), `a8766b2` (the pane branch merged at `a6efa54`: the isolate fix, WIRE, the ruler rows and its launch through `glasshouse launch --profile`), `e3297cb` (61E LOCALLY VERIFIED). The account's five-hour window stopped every pane 03:12–05:15; the third lead exited to a shell after its handoff and a fourth was relaunched over it. Live: the credential-strip worker on its second fix-forward (the verifier ACCEPTED the strip at all five sites and REJECTED the package because the harness's hooks — memory extraction, the firewall reducer — inherit the stripped environment and lose an environment-only key; the fix is a loud notice in both hooks and in `doctor` pointing at the keychain), its verifier held for the re-check, the pane lead with two subs, and `GH-SECRET-LEAK-GUARD` (a pre-commit/pre-push/lint scanner for key values and key shapes, the user's instruction at 03:00 — the sixteen local key values were first shown absent from the tree and the whole history).
- **Ruled:** no credential channel from the launcher to its hooks — the keychain is 488's secure-store boundary; parked with the user in product terms (`provider-keys-and-helpers`). The benchmark needs no standing gateway: each attempt runs through `glasshouse launch --profile … -- <argv>` in its own worktree and reads its own ledger; a router key first, the subscription run parked (`pane-benchmark-account`).
- **Dogfooding (2026-09-06):** the session's five findings recorded; the *N tasks completed* line counts task boundaries, not turns; extraction routed to groq and left no durable outcome (successor `GH-EXTRACTION-OUTCOME-OBSERVED`).
- **Trailing sweep:** `34001464037` red on two cells only — an events-log ordering test on windows-latest declared (strike 1; `34002304272` is the rerun) and one pane sandbox test (the lead's, decision 4).
- **Next, by risk:** the strip's re-check and 488's re-tick; the guard's integration and `install-git-hooks.sh` on main; the lead's merge ask and the benchmark; the sixteen disputed census rows (rulings drafted in `.agent-runtime/CONTINUATION.md` §3); the `NothingConfigured` sentence.

## Checkpoint — 2026-09-06 (02:35), waves 140–141: 1331 / 1413 (94.2%) — Phase 60 complete, the census and the user's rulings move the map, two dogfooding defects, the site in the user's words

- **State:** `main` at `4ec2ec6`, pushed. Phase 60 complete (`4b70d72`); Phase 61's 61D+61E merged (`221e355`); direct verified completion specified and authorised (`26ed07a`, line 2485); seven gates adopted (`064f19b`); ten lines decided-out (`7c911bd`); **Phase 62** recorded from fourteen promoted parked lines (`961b7c6`); 488 un-ticked on a dogfooding finding (`88d1a7f`), its Red fix built and under independent verification. Live: the third pane lead with its subs (WIRE — pane's first real task — then the ruler's two-column run), the credential-strip verifier, the dogfooding session.
- **Ruled:** the user's seventeen yes/no answers (`design-decisions.md`, *Steering decisions of record — 2026-09-06*, *Seven experiment gates*, *Fourteen parked lines promoted*, *Direct verified completion*). Benchmark data is the milestone after WIRE (`ruling-benchmark-first.md`).
- **Trailing sweeps:** glasshouse cells green on `17599f3`; the pane windows cell's verdict with the cherry-picked fixture repairs is `34001464037`.
- **Next, by risk:** verifier ACCEPT → integrate the strip, re-tick 488; the lead's isolate merge and WIRE; the sixteen disputed census rows, one ruling each; the `NothingConfigured` message; blast-radius runs the Windows-target check itself; the pre-release once the pane cell is green (decision 10).

> Checkpoints before 2026-09-06 (01:30) — waves up to 139 — and the sections that followed them (current capability, verified work, loose ends, next steps as of that hand-off) moved to docs/history/handoff-2026-08-27-to-2026-09-06.md.
