/target
/.agent-runtime/

# Python bytecode from repository tooling.
__pycache__/
*.pyc

# Worker worktrees live inside the repo but are never part of it.
# scripts/ci-local.sh excludes this from the tree it ships to the container.
.worktrees/
